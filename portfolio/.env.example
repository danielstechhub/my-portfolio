# Copy this file to ".env" and adjust for your environment.
# On a stock local XAMPP install the defaults below already work — you only
# need a .env file at all if something differs (e.g. you set a MySQL password).

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=portfolio_db
DB_USER=root
DB_PASS=

CONTACT_EMAIL_TO=hello@ayodejidaniel.dev
CONTACT_EMAIL_FROM=noreply@ayodejidaniel.dev

SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=tls
